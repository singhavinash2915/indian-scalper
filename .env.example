# Copy to .env and fill in. Never commit .env.
# Required only when broker: upstox in config.yaml.

UPSTOX_API_KEY=
UPSTOX_API_SECRET=
UPSTOX_ACCESS_TOKEN=

# Required safety gate before mode: live is allowed.
LIVE_TRADING_ACKNOWLEDGED=no
